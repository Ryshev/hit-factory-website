RewriteEngine On
ErrorDocument 404 /404.html
Options -Indexes

# Force HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}/$1 [R=301,L]

# Redirect old locale slugs to new ones
RewriteRule ^uk/(.*)$ /ua/$1 [R=301,L]
RewriteRule ^ka/(.*)$ /ge/$1 [R=301,L]
RewriteRule ^hy/(.*)$ /am/$1 [R=301,L]
RewriteRule ^kk/(.*)$ /kz/$1 [R=301,L]

# Auto-detect language from Accept-Language header (only for root /)
RewriteCond %{REQUEST_URI} ^/$
RewriteCond %{HTTP:Accept-Language} ^ru [NC]
RewriteCond %{HTTP_COOKIE} !hf-locale=
RewriteRule ^$ /ru/ [R=302,L]

RewriteCond %{REQUEST_URI} ^/$
RewriteCond %{HTTP:Accept-Language} ^uk [NC]
RewriteCond %{HTTP_COOKIE} !hf-locale=
RewriteRule ^$ /ua/ [R=302,L]

RewriteCond %{REQUEST_URI} ^/$
RewriteCond %{HTTP:Accept-Language} ^ka [NC]
RewriteCond %{HTTP_COOKIE} !hf-locale=
RewriteRule ^$ /ge/ [R=302,L]

RewriteCond %{REQUEST_URI} ^/$
RewriteCond %{HTTP:Accept-Language} ^hy [NC]
RewriteCond %{HTTP_COOKIE} !hf-locale=
RewriteRule ^$ /am/ [R=302,L]

RewriteCond %{REQUEST_URI} ^/$
RewriteCond %{HTTP:Accept-Language} ^kk [NC]
RewriteCond %{HTTP_COOKIE} !hf-locale=
RewriteRule ^$ /kz/ [R=302,L]

# Security: block admin data files
RewriteRule ^admin/data/ - [F,L]

# Cache static assets
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType image/avif "access plus 1 year"
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType image/svg+xml "access plus 1 year"
  ExpiresByType text/css "access plus 1 month"
  ExpiresByType application/javascript "access plus 1 month"
</IfModule>

# Compression
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/css application/javascript application/json image/svg+xml
</IfModule>
